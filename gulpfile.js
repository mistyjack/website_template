const path = require("path");

require(path.join(__dirname, 'gulp', 'tasks', 'styles'));
require(path.join(__dirname, 'gulp', 'tasks', 'watch'));
require(path.join(__dirname, 'gulp', 'tasks', 'scripts'));
require(path.join(__dirname, 'gulp', 'tasks', 'sprites'));
require(path.join(__dirname, 'gulp', 'tasks', 'modernizr'));
require(path.join(__dirname, 'gulp', 'tasks', 'build'));
